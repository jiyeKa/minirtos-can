# --- 자동 생성물: 소스에서 언제든 다시 만들어지므로 추적하지 않는다 ---
build/
managed_components/
dependencies.lock

# --- 내 PC에서만 유효한 설정: 절대경로가 박혀 있어 공유하면 안 된다 ---
.vscode/
.clangd
compile_commands.json

# --- ESP-IDF 설정 백업본 ---
sdkconfig.old

# --- OS 부스러기 ---
Thumbs.db
desktop.ini
.DS_Store
